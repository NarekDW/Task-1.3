version = '1.0-SNAPSHOT'

subprojects{
    apply plugin: 'java'

    sourceCompatibility = JavaVersion.VERSION_1_8

    repositories{
        mavenCentral()
    }
}

task wrapper(type: Wrapper){
    gradleVersion = '3.2.1'
}